% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch.R
\name{launch}
\alias{launch}
\title{Launch the shinyFiles Demo Application}
\usage{
launch(
  port = NULL,
  host = "127.0.0.1",
  launch.browser = TRUE,
  volumes = NULL,
  theme = NULL,
  run = "w",
  ...
)
}
\arguments{
\item{port}{The port number for the Shiny application (default: NULL for
auto-select)}

\item{host}{The host IP address (default: "127.0.0.1" for localhost)}

\item{launch.browser}{Whether to launch the application in a browser
(default: TRUE)}

\item{volumes}{Named list of custom volumes for file operations. If NULL,
default volumes based on the operating system will be used.}

\item{theme}{Optional bslib theme to override the default theme}

\item{run}{Display mode for the app when running in RStudio:
\itemize{
\item \code{"p"} = launch in viewer pane
\item \code{"b"} = launch in external browser
\item \code{"w"} = launch in window (default)
}}

\item{...}{Additional arguments passed to shinyApp()}
}
\value{
A Shiny app object
}
\description{
Standalone function to launch the shinyFiles demonstration application.
This function combines the UI and server components and provides additional
configuration options.
}
\examples{
if (interactive()) {
  # Basic launch
  launch()
  
  # Launch with custom port
  launch(port = 8080)
  
  # Launch in RStudio viewer pane
  launch(run = "p")
  
  # Launch in external browser
  launch(run = "b")
  
  # Launch with custom volumes
  custom_volumes <- c("Data" = "/path/to/data", "Projects" = "/path/to/projects")
  launch(volumes = custom_volumes)
  
  # Launch with custom theme
  launch(theme = bslib::bs_theme(bootswatch = "darkly"))
}
}
